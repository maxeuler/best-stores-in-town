enum Tag {
  VEGETARIAN
  FAMILYFRIENDLY
  HEALTHY
  FASTFOOD
  FINEDINING
}

type User {
  id: ID! @id
  name: String!
}

type Store {
  id: ID! @id
  name: String!
  description: String!
  image: String
  largeImage: String
  tags: [Tag]! @scalarList(strategy: EMBEDDED)
}